# E‑Commerce — Admin Frontend (Vite + React)

Admin CMS for products, orders, and review moderation.

## ✨ Features

- Admin auth
- Products CRUD
- Orders list & status updates
- Review moderation

## 🧱 Tech Stack

- **React + Vite**
- **UI library:** react-bootstrap / bootstrap / bootstrap-icons
- **Packages used:** `@reduxjs/toolkit`, `axios`, `bootstrap`, `bootstrap-icons`, `moment`, `react`, `react-bootstrap`, `react-dom`, `react-icons`, `react-redux`, `react-router-dom`, `react-toastify`, `@eslint/js`, `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `eslint`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`, `globals`, `vite`

## 📂 Project Structure (typical)

```
src/
  components/
  pages/
  hooks/
  utils/
  App.jsx
  main.jsx
public/
```

## ⚙️ Environment

**.env.example**

```
VITE_APP_API_URL=http://localhost:4000
```

## ▶️ Getting Started

```bash
yarn install
yarn dev     # http://localhost:5174
```
